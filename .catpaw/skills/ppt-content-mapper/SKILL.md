# PPT Content Mapper

Use this skill to map approved outline content into reviewable `slide_contents.json`.

Rules:

- Preserve titles and bullets as editable text zones.
- Keep image references separate from factual text.
- Set draft review status until the user approves content.
- Carry source references and fallback flags forward.
